<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>{{ title }} | {{ sitename }}</title>
  </head>
  <body>
    <h1>{{ title }}</h1>
    {{#if category}}
    <p>
      <small>In category
        <a href="/blog/category/{{ category }}">{{ category }}</a>
      </small>
    </p>
    {{/if}} {{{ contents }}}
    <hr />
    {{#if tags}} Tagged: {{#each tags}}
    <a href="/blog/tags/{{ . }}">{{ . }}</a>, {{/each}} {{/if}}
  </body>
</html>
